# 更新日志

本项目的显著变更记录在此文件中。

## v1.7.0 - 2026-09-14

参考 `astrbot_plugin_buckshot_roulette` 重做菜单与房间逻辑，并修复会误触发默认 LLM 的缺陷。

### 修复：指令不再漏进默认 LLM

- 之前插件用 `bot.api.post_group_message` 直发消息且从不 `stop_event`，AstrBot 的判定是「插件调用过 `event.send` 或事件被 stop」才跳过默认 LLM，因此**文本指令会同时触发一次 LLM 回复**（带图片的「帮助」因为走了 `event.send` 反而正常）。
- 现在所有分支结束都会 `stop_event()` 并 `should_call_llm(True)`，与 `astrbot_plugin_buckshot_roulette` / `astrbot_plugin_util` 的做法一致。
- 无空格写法（如 `百万美金创建`、`百万美金退出房间`）注册为命令别名，不再因为匹配不上过滤器而漏进 LLM；同时给指令加上群聊事件过滤。

### 新增：房间管理

- `百万美金 退出房间`：仅大厅阶段可用；最后一人退出时房间自动关闭；首领退出时首领转交给剩余第一位玩家。
- `百万美金 关闭房间`（`关闭` / `结束` / `解散` 同义）：首领或 AstrBot 管理员可用，任意阶段都能关闭。
- 大厅创建、加入、退出、状态、关闭的回复统一显示**人数计数**（如 `3/8 人`），人数不足时提示还差几人。

### 菜单

- 参考恶魔轮盘插件：Markdown 说明 + 5 行按钮组，按钮文案即 `visited_label`，`action.data` 等价于可手动输入的文本指令。
- 第一行为房间管理：创建房间 / 加入 / 退出房间 / 关闭房间；其余四行为开始游戏 / 查看状态、转账 / 退出本轮、准备 / 取消准备、使用威胁牌 / 帮助（规则卡）。
- `ButtonSpec` 新增 `row` 字段，`build_keyboard` 支持显式分行（每行最多 5 个、最多 5 行、总计 25 个按钮）。
- 规则速览补充常用指令清单；未知子指令提示复用菜单文案。

### 玩家可见文案

- 阶段不再直接显示内部英文取值（如 `role_selection`），改为中文：大厅 / 选角 / 谈判 / 告密人指定 / 抢劫结算 / 回合结束 / 游戏结束。
- 去掉面向玩家的实现术语「槽位」（改为「人物」）；状态里的回合改为「第 N 回合 / 共 8 回合」，人数改为「3/8」。
- 大厅阶段玩家状态显示「已加入」，不再错误地显示「已退出」；未开局时不显示回合行。
- 名字后的多余空格、状态列表分隔符等排版细节一并清理。

### 测试

- 新增退出房间（含房主转交、空房自动关闭、非大厅拒绝）、关闭房间（首领/管理员/无房间/进行中）、人数计数、菜单版式、显式分行、`stop_event` 防 LLM、无空格别名的用例；测试总数达到 163 条。
- metadata 与插件注册版本号升级到 v1.7.0。

## v1.6.2 - 2026-09-14

- 揭露图改为渲染本轮**中心牌堆**：每个提交过角色的槽位一张牌，被随机隐藏的那张用**卡背**代替，其余为角色卡。
- 谈判期退出的玩家，其角色牌仍匿名留在中央牌堆，因此重新纳入揭露图（牌堆不标注归属，不泄露玩家与角色的对应关系）。
- 合并支持自动换行：每行最多 5 张，8 人局为两行，输出仍是同一张图片。
- 修复告密人指定角色后会重复执行一遍抢劫结算、导致对局卡在 `snitch_selection` 阶段无法结算的缺陷；改为一轮只结算一次，指定后仅做收尾。
- `Reply.reveal_roles` 更名为 `Reply.reveal_cards`（现在承载整堆卡面，含卡背键）。
- 新增揭露堆含卡背、告密人指定延后发图、告密人指定后仍能正常收尾的测试；测试总数达到 142 条。
- metadata 与插件注册版本号升级到 v1.6.2。

## v1.6.1 - 2026-09-14

- 修正素材分类：`docs/sources/role-cards/06_the_munday_family.jpg` 实际是**角色卡卡背**（牌面印有帮派名），改名为 `card-back.jpg` 并在注册表中登记为 `CARD_BACK_PATH`。
- 同步修正归档的 `docs/sources/role-cards/cards.json`（第 6 张 `type` 改为 `card_back`）。
- 新增卡背文件存在性测试；更新 README 与 metadata 版本号到 v1.6.1。

## v1.6.0 - 2026-09-14

- 新增帮助接口：`百万美金 帮助`（`规则`、`规则卡` 同义）输出规则卡图片与规则速览，图片缺失时只回复文字并提示。
- 新增身份揭露卡图：每轮抢劫结算时，把本轮参与抢劫的角色卡合并成**一张**图片发送，卡片顺序每次随机打乱，避免固定顺序暗示玩家与角色对应关系。
- 谈判期已退出的槽位不会出现在揭露图中（其角色在本轮仍需保密）。
- 新增 `game/help.py`（规则卡/角色卡注册表）与 `game/cards.py`（Pillow 卡图合成，系统存在中文字体时自动为卡片加中文名）。
- `Reply` DTO 新增 `images` 与 `reveal_roles` 字段，图片路径使用相对插件根目录的形式以便进入幂等缓存。
- 归档角色卡素材到 `docs/sources/role-cards/`，规则卡图片移动到 `docs/sources/rule-cards/rule-card.jpg`。
- 新增 `tests/test_cards.py` 与 QQ 适配层图片发送用例；测试总数达到 139 条。
- 更新 README 的指令表、卡图说明与代码结构；metadata 与插件注册版本号升级到 v1.6.0。

## v1.5.0 - 2026-09-14

- 完成赃物牌组逐张核验：录入 10 张实物牌面的赃款、保证金与奖励角色，`LOOT_DECK_VERIFIED` 置为 True，插件现在可以正常开局。
- 新增 `docs/sources/loot-cards/`：归档 10 张牌面截图、牌背与识别结果 `cards.json`，并补充中文规则书总览图。
- 新增 `tests/test_loot_deck.py`：逐张固定 10 张牌面，交叉校验 `cards.json` 与 `loot.py` 常量，校验来源索引文件存在与牌组分布（8×4/9×3/10×2/12×1、保证金 1×7/2×3）。
- 新增真实牌组下的整轮与全局联调用例：使用已核验牌组跑完一轮，并连续跑到第 8 回合结束、判定胜者。
- 重写《赃物牌组核验记录》，记录核验方法（数字逐位放大比对 + 角色符号色相分析）与逐张结果。
- 新增 main.py 真实路由下的开局联调用例（创建 → 加入 → 开始 → 状态）。
- 测试总数达到 120 条；更新 README 的可玩状态与牌面表格，metadata 与插件注册版本号升级到 v1.5.0。

## v1.4.0 - 2026-09-14

- 实现 QQ 官方 Bot 可玩流程：创建/加入/开始/状态/菜单，选角、谈判、独立转账、独立退出、准备、强制抢劫、使用威胁牌与 HMAC 操作令牌。
- 新增 `game/models.py` 领域模型与严格快照序列化，金额一律使用整数百万美元。
- 新增 `game/rules.py` 纯规则函数：角色集合、3 人双槽位、保证金与储备区补足、公开角色计数、抢劫固定结算顺序、分赃顺序、即时胜利与第 8 回合判定。
- 新增 `game/tokens.py` HMAC 动作协议：规范载荷、32 字节密钥文件（0600）、43 字符令牌、候选枚举匹配与代次失效。
- 新增 `game/repository.py` SQLite 仓储：`meta`/`games`/`processed_messages` 三表、立即写事务、消息幂等与快照恢复。
- 新增 `game/service.py` 房间服务：房间锁、事务边界、幂等结果、三种状态流转与全部指令用例。
- 新增 `game/qqofficial.py` QQ 适配层：可信身份提取、type=2 按钮（公开/指定用户）、keyboard 分页、被动回复上下文与发送失败处理。
- 重写 `main.py` 为插件注册与命令路由，命令与按钮共用同一套服务端校验。
- `game/loot.py` 提供赃物牌常量与合法性校验：10 张、金额 800 万～1200 万、保证金 100 万或 200 万、奖励角色必须合法。牌面尚未逐张核验前拒绝开局。
- 新增 109 条 pytest 用例，覆盖令牌、规则、仓储、服务、QQ 适配与入口契约。
- 记录 v1.4.0 实现补充约定：保证金退款口径、强制抢劫效果、退出清空准备、按钮上限、告密人目标来源、威胁牌使用时机。
- 新增《赃物牌组核验》文档，说明牌面核验步骤与来源索引要求。
- 更新 README 的中文玩法说明、代码结构与测试方式；忽略本地运行产物。

## v1.3.3 - 2026-09-14

- 新增 QQ 官方 Bot 游戏开发实现设计，冻结 2016 初版的状态机、领域模型、HMAC 协议、SQLite 事务及测试验收矩阵。
- 将谈判实现修订为 QQ 群内自由交流；插件只提供彼此独立的原子转账和原子退出，不保存报价或接受状态。
- 将威胁牌修订为仅持牌人可用的 type=2 明文身份按钮，不再依赖 C2C，并记录客户端侧保密边界和发送失败补偿。
- 同步修订 QQ 官方 Bot 开发说明、README 和文档索引。

## v1.3.2 - 2026-09-14

- 新增 QQ 官方 Bot 开发说明，整理 Markdown 按钮、指令按钮、指定用户按钮、回调按钮和被动回复上下文。
- 补充 AstrBot QQOfficial 事件判断、openid 提取、按钮调试日志与持续开发流程，方便后续游戏指令和按钮菜单开发。
- 在项目 README 与 docs README 中增加 QQ 官方 Bot 开发文档入口。

## v1.3.1 - 2026-09-14

- 拉取并归档《百万美金》2016 初版与 2024 二版英文规则书。
- 新增以 2016 初版为基线的完整中文规则文档。
- 补充版本差异、资料来源和版权说明。
- 新增规则书文件完整性及中文规则结构测试。
- 将项目 README 更新为中文，并标明当前开发状态。
- 修正模板遗留的插件元数据，确保本地安装包名称有效。
